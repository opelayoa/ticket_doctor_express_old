<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay"
           parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="StoreButton"
           parent="@android:style/Widget.Holo.Light.Button.Borderless.Small">
        <item name="android:gravity">center</item>
        <item name="android:drawablePadding">10dp</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="EditNotesButton"
           parent="@android:style/Widget.Holo.Light.Button.Borderless.Small">
        <item name="android:layout_width">25dp</item>
        <item name="android:layout_height">25dp</item>
        <item name="android:scaleType">fitCenter</item>
    </style>

    <!--<style name="TicketDetail" parent="android:Widget.TextView">-->
    <style name="TicketDetail" parent="@android:style/TextAppearance">
        <item name="android:textSize">@dimen/text_size_medium_material</item>
        <item name="android:textColor">@color/ticket_detail_left</item>
        <item name="android:textStyle">bold</item>
    </style>
    <style name="TicketDetailValue"  parent="@android:style/TextAppearance">
        <item name="android:textSize">@dimen/text_size_medium_material</item>
        <item name="android:textColor">@color/ticket_detail_value</item>
    </style>

    <!--<style name="SearchView" parent="AppTheme">-->
        <!--<item name="android:searchViewSearchIcon">@android:drawable/ic_search</item>-->
    <!--</style>-->

</resources>
