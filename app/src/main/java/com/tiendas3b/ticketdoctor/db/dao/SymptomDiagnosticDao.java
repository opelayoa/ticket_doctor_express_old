package com.tiendas3b.ticketdoctor.db.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.tiendas3b.ticketdoctor.db.dao.SymptomDiagnostic;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SYMPTOM_DIAGNOSTIC".
*/
public class SymptomDiagnosticDao extends AbstractDao<SymptomDiagnostic, Long> {

    public static final String TABLENAME = "SYMPTOM_DIAGNOSTIC";

    /**
     * Properties of entity SymptomDiagnostic.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Sub = new Property(1, boolean.class, "sub", false, "SUB");
        public final static Property Status = new Property(2, boolean.class, "status", false, "STATUS");
        public final static Property Renew = new Property(3, String.class, "renew", false, "RENEW");
        public final static Property DiagnosticId = new Property(4, long.class, "diagnosticId", false, "DIAGNOSTIC_ID");
        public final static Property SymptomId = new Property(5, long.class, "symptomId", false, "SYMPTOM_ID");
    };

    private DaoSession daoSession;


    public SymptomDiagnosticDao(DaoConfig config) {
        super(config);
    }
    
    public SymptomDiagnosticDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SYMPTOM_DIAGNOSTIC\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"SUB\" INTEGER NOT NULL ," + // 1: sub
                "\"STATUS\" INTEGER NOT NULL ," + // 2: status
                "\"RENEW\" TEXT NOT NULL ," + // 3: renew
                "\"DIAGNOSTIC_ID\" INTEGER NOT NULL ," + // 4: diagnosticId
                "\"SYMPTOM_ID\" INTEGER NOT NULL );"); // 5: symptomId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SYMPTOM_DIAGNOSTIC\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, SymptomDiagnostic entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getSub() ? 1L: 0L);
        stmt.bindLong(3, entity.getStatus() ? 1L: 0L);
        stmt.bindString(4, entity.getRenew());
        stmt.bindLong(5, entity.getDiagnosticId());
        stmt.bindLong(6, entity.getSymptomId());
    }

    @Override
    protected void attachEntity(SymptomDiagnostic entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public SymptomDiagnostic readEntity(Cursor cursor, int offset) {
        SymptomDiagnostic entity = new SymptomDiagnostic( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getShort(offset + 1) != 0, // sub
            cursor.getShort(offset + 2) != 0, // status
            cursor.getString(offset + 3), // renew
            cursor.getLong(offset + 4), // diagnosticId
            cursor.getLong(offset + 5) // symptomId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, SymptomDiagnostic entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setSub(cursor.getShort(offset + 1) != 0);
        entity.setStatus(cursor.getShort(offset + 2) != 0);
        entity.setRenew(cursor.getString(offset + 3));
        entity.setDiagnosticId(cursor.getLong(offset + 4));
        entity.setSymptomId(cursor.getLong(offset + 5));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(SymptomDiagnostic entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(SymptomDiagnostic entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getDiagnosticDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getSymptomDao().getAllColumns());
            builder.append(" FROM SYMPTOM_DIAGNOSTIC T");
            builder.append(" LEFT JOIN DIAGNOSTIC T0 ON T.\"DIAGNOSTIC_ID\"=T0.\"_id\"");
            builder.append(" LEFT JOIN SYMPTOM T1 ON T.\"SYMPTOM_ID\"=T1.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected SymptomDiagnostic loadCurrentDeep(Cursor cursor, boolean lock) {
        SymptomDiagnostic entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Diagnostic diagnostic = loadCurrentOther(daoSession.getDiagnosticDao(), cursor, offset);
         if(diagnostic != null) {
            entity.setDiagnostic(diagnostic);
        }
        offset += daoSession.getDiagnosticDao().getAllColumns().length;

        Symptom symptom = loadCurrentOther(daoSession.getSymptomDao(), cursor, offset);
         if(symptom != null) {
            entity.setSymptom(symptom);
        }

        return entity;    
    }

    public SymptomDiagnostic loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<SymptomDiagnostic> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<SymptomDiagnostic> list = new ArrayList<SymptomDiagnostic>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<SymptomDiagnostic> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<SymptomDiagnostic> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
